// Este archivo existe solo para que Vercel detecte Next.js en la raíz del monorepo.
// El build real ocurre en frontend/ via vercel.json buildCommand.
/** @type {import('next').NextConfig} */
const nextConfig = {};
export default nextConfig;
